// Navigation height constant
// Nav component uses h-16 = 64px
export const NAV_HEIGHT_PX = 64;

// Additional offset for comfortable spacing
export const NAV_SCROLL_OFFSET_PX = 12;

// Total scroll offset to use for scroll-margin-top
export const TOTAL_SCROLL_OFFSET_PX = NAV_HEIGHT_PX + NAV_SCROLL_OFFSET_PX;
